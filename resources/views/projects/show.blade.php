<x-layout>
    <h1>{{ $project['title'] }}</p>
    {{-- <ul class="mt-4 list-disc list-inside">
        @foreach ($projects as $project)
            <li><a href="">{{ $project['title'] }}</li>
        @endforeach
    </ul> --}}
</x-layout>